{
  "name": "shell",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bundle": "node_modules/.bin/parcel build main.js",
    "minify": "node_modules/.bin/terser --compress --mangle -o ../assets/shell.js -- dist/main.js",
    "build": "yarn bundle && yarn minify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.0.0-beta.2",
    "terser": "^5.6.1"
  },
  "dependencies": {
    "bash-parser": "^0.5.0"
  }
}
