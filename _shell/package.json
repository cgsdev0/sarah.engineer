{
  "name": "shell",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bundle": "mkdir -p dist; node_modules/.bin/browserify main.js -o dist/main.js",
    "minify": "node_modules/.bin/terser --compress --mangle -o ../assets/shell.js -- dist/main.js",
    "build": "yarn bundle && yarn minify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^17.0.0",
    "terser": "^5.6.1"
  },
  "dependencies": {
    "bash-parser": "^0.5.0"
  }
}
